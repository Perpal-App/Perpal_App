{
  "name": "perpal-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "PerPal RPC gateway. Keeps provider credentials off the device.",
  "scripts": {
    "dev": "wrangler dev --env mainnet",
    "typecheck": "tsc --noEmit",
    "deploy:mainnet": "wrangler deploy --env mainnet",
    "secrets:mainnet": "wrangler secret list --env mainnet",
    "tail:mainnet": "wrangler tail --env mainnet"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260804.1",
    "typescript": "~6.0.3",
    "wrangler": "4.118.0"
  }
}
